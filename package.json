{
  "name": "in-young-supplements",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  }
}
